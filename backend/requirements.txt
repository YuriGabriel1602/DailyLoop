fastapi
uvicorn
sqlmodel
passlib[bcrypt]
python-jose[cryptography]
python-multipart
bcrypt==3.2.0

# IA (chat Prometheus) — motor plugável
google-genai
openai
anthropic

# Financeiro (categorização + import de extrato OFX)
scikit-learn
ofxparse

# Notificações (WhatsApp Business Cloud API) + agendamento
httpx
apscheduler

# CRM / Inbox omnichannel (WebSocket em tempo real)
websockets

# Config
python-dotenv
